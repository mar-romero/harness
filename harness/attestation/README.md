# Attestation workspace

- `README.md`: explains that R3 attestation keys and signed output are external/runtime concerns.

Do not store private keys in this repository. `scripts/attest.py` writes provenance using keys outside the repository keyspace.
