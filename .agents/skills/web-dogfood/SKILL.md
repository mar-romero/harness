---
name: web-dogfood
description: Exercise a web product from the user-visible boundary and record reproducible failures.
---

# web-dogfood

Use the actual UI/API path when available. Start from a clean state, record environment, steps and expected/actual behavior, capture only necessary artifacts, and distinguish product defects from environment/tool failures. Never use production credentials without approval.
