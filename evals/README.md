# Harness evaluations

This folder contains deterministic evaluation inputs for routing and gate behavior; it is not a normal product-task directory.

- `cases/`: JSON scenarios for routing and dangerous-command decisions.
- `README.md`: explains the evaluation boundary.
