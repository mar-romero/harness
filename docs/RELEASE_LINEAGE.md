# Release lineage

This final source tree is already merged; do not re-apply the older ZIPs.

1. **v2 core:** canonical manifest, router, executable gates, evals, worktrees,
   context compiler, capability agents, evidence ledger.
2. **v3 changes 9–10:** model router and proposal-only harness evolution.
3. **OpenCode v3 overlay:** OpenCode primary orchestrator, least-privilege
   generated agents, native plugin gates, live model catalog/inventory and task
   activation.
4. **Final research-driven layer:** canonical-English request boundary,
   structured multilingual risk, runtime eval lab, typed handoffs, progress
   ledger, Ed25519 attestations, context graph/verified memory and
   champion/challenger experiments.
