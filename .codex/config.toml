[mcp_servers.harness-aci]
command = "powershell.exe"
# Project MCP paths are resolved from this config directory.  `..` selects the
# repository root, so the relative launcher works when Desktop starts elsewhere.
args = ["-NoLogo", "-NoProfile", "-NonInteractive", "-ExecutionPolicy", "Bypass", "-File", "scripts/start_aci_mcp.ps1"]
cwd = ".."
enabled = true
required = true
startup_timeout_sec = 30
tool_timeout_sec = 180

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
