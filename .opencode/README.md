# OpenCode adapter

This folder integrates the harness with OpenCode.

- `agents/`: generated roles and the detailed orchestrator adapter.
- `commands/`: request, task, and model commands.
- `plugins/harness/`: runtime context, model, permission, and audit integration.
- `README.md`: explains this adapter boundary.

Global permissions are in `opencode.json`; edit canonical sources instead.
